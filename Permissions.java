package application;

/**
 * This Enum holds the four types of permissions possible for a user to have
 * in ClinicMate.
 *
 */
enum Permissions {
    ADMIN,
    NURSE,
    DOCTOR,
    PATIENT,
}
